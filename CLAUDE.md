# שיפוצים לי — מדריך תחזוקה

אתר תוכן שיווקי לשיפוצים בעברית (RTL). Next.js App Router + TypeScript + Tailwind v4 + MDX.
כל התוכן חי בקבצי MDX; הקוד לא מכיל תוכן קשיח.

## כלל ברזל: אסור לשנות כתובות URL

**אסור בתכלית האיסור לשנות slug של עמוד קיים** — לא בשינוי שם קובץ MDX, לא בהעברה בין תיקיות content, ולא בשינוי נתיבים בקוד. שינוי כתובת מחזיר 404 לכל מי שהגיע מגוגל, מקישור חיצוני או מסימנייה, ומוחק את הוותק של העמוד באינדקס. זה חל על כל הכתובות הקיימות ועל כל כתובת חדשה מרגע שנוצרה ונפרסה. אם אין ברירה ויש צורך עסקי אמיתי בשינוי — חובה להתייעץ עם בעל האתר קודם ולהקים redirect 301 מהכתובת הישנה.

## כלל ברזל: אין להעלות עמוד עם תוכן כפול

**לפני יצירת כל עמוד חדש - חובה לבדוק שאין כבר עמוד שמכסה את אותה כוונת חיפוש, ואם התוכן ייצא כפול או דומה מדי, לא מעלים אותו.** גוגל מעניש (Helpful Content) אתרים עם קניבליזציה ותוכן דק/משוכפל, וזה פוגע בכל האתר, לא רק בעמוד הבודד.

**בדיקת חובה לפני יצירת עמוד (עצור אם אחד מאלה מתקיים):**
1. עבור על כל ה-`keyword` וה-`title` הקיימים ב-`content/services/*.mdx` ו-`content/pages/*.mdx`. אם כבר קיים עמוד עם אותו ביטוי מרכזי או ביטוי נרדף (למשל "שיפוץ בית" מול "שיפוץ בית פרטי", "מחירון צבע" מול "מחירון עבודות צבע") - **אל תיצור עמוד חדש.**
2. אם שתי כוונות החיפוש כמעט זהות (גולש שמחפש את X ואת Y מצפה לאותה תשובה) - זו כפילות. אל תעלה.
3. אם התוכן החדש היה נאלץ לחזור על מחירים, FAQ או פסקאות של עמוד קיים כדי למלא 700 מילים - זה סימן שאין לו זכות קיום עצמאית.

**מה עושים במקום:**
- אם העמוד הקיים חלש - **משפרים ומרחיבים אותו**, לא יוצרים מתחרה.
- אם באמת יש זווית מובחנת וייחודית (תת-נושא, עיר, סוג עבודה ספציפי) - יוצרים אותו רק אם הוא **מבודל לחלוטין** בכותרת, ב-FAQ ובגוף, עם קישור הדדי לעמוד הקרוב לו, בלי חפיפת פסקאות.
- במקרה גבולי - **עוצרים ומתייעצים עם בעל האתר** לפני העלאה, לא מהמרים.

הכלל חל גם על יצירה בכמות (סוכנים/סקריפטים): כל עמוד בקבוצה חייב לעבור את הבדיקה הזו מול הקיימים ומול האחרים באותה קבוצה.

## מבנה

- `content/services/<slug>.mdx` — עמודי שירות (שם הקובץ = ה-slug = ה-URL)
- **ה-slugs באנגלית בלבד (ASCII), בתרגום משמעות הביטוי המרכזי** — למשל `apartment-renovation`, `house-painting`, `roof-waterproofing`. לא תעתיק ולא עברית (שמות קבצים בעברית שברו את הפריסה ב-Vercel)
- `content/pages/<slug>.mdx` — עמודי מידע (אודות, נגישות, פרטיות)
- `content/areas/<slug>.mdx` — אזורי שירות (עתידי, הנתיב כבר נתמך)
- `content/TEMPLATE.mdx` — תבנית לעמוד חדש
- `src/config/site.ts` — הקובץ המרכזי: פרטי עסק, קבוצות שירותים (סיידבר/פוטר/דף הבית), אזורי שירות
- `src/app/[slug]/page.tsx` — התבנית שמרנדרת כל עמוד תוכן (breadcrumbs, סיידבר, מחירים, FAQ, JSON-LD)
- `src/lib/content.ts` — טעינת MDX + טיפוס ה-frontmatter

## פעולות נפוצות

**הוספת עמוד שירות:** קודם כול - בצע את בדיקת הכפילות (ראה "כלל ברזל: אין להעלות עמוד עם תוכן כפול"). רק אם עבר: העתק את `content/TEMPLATE.mdx` אל `content/services/<slug>.mdx`, מלא frontmatter וגוף (700+ מילים), הוסף את ה-slug לקבוצה מתאימה ב-`src/config/site.ts` (serviceGroups), והוסף 2+ קישורים אליו מגוף עמודים קיימים רלוונטיים (`[טקסט](/slug)`).

**עדכון מחירים:** ערוך את `prices.rows` ב-frontmatter של העמוד. עדכן גם את `updated: "YYYY-MM"` (אם השדה חסר — מוצג חודש הבנייה האחרון אוטומטית).

**שינוי סיידבר של עמוד:** שדה `sidebar` ב-frontmatter (רשימת slugs).

**היררכיה (עמודי קטגוריה):** שדה `parent: "<slug>"` ב-frontmatter משבץ את העמוד תחת עמוד-אב ב-breadcrumbs (וב-BreadcrumbList JSON-LD). עמודי הקטגוריה הקיימים: `renovation-contractors` (כל הערים), `professionals` (בעלי מקצוע), `outdoor-renovation` (חוץ וגינה). עמוד עיר חדש מקבל תמיד `parent: "renovation-contractors"` וקישור מגוף עמוד ההאב.

**הוספת אזור שירות:** צור `content/areas/<slug>.mdx`, הוסף קבוצה ב-`serviceAreas` ב-site.ts, והוסף את ה-slug לשדה `areas` ב-frontmatter של השירותים הרלוונטיים — הסיידבר יציג אותו אוטומטית.

## צ'ק-ליסט עמוד תוכן (חובה בכל עמוד חדש ובכל אופטימיזציה)

המטרה: כל עמוד ברמה הגבוהה ביותר, ייחודי ב-100%, מותאם למנועי חיפוש ולמנועי AI (AIO), ומייצר אמון עמוק מול הלקוח וגוגל. `[אוטו]` = נאכף אוטומטית ע"י התבנית ואין צורך לכתוב ידנית. `[פער]` = סטנדרט שעדיין לא מיושם באתר, לסגור כשיהיו נכסים.

### 1. אסטרטגיה וייחודיות
- **ביטוי מרכזי** בשדה `keyword`, ומופיע במדויק **6+ פעמים** בגוף - טבעי לחלוטין, בלי דחיסת מילות מפתח.
- **נרטיב סמנטי:** לשלב וריאציות ומונחים משלימים (בעמוד "צביעת דירה": סוגי צבע, הכנת קירות ושפכטל, צבעי מקצועי, מחיר למ״ר).
- **אפס כפילות** - ראה כלל הברזל למעלה. בעמודי עיר/אזור אסורה תבנית גנרית עם החלפת שם עיר; מחירון ותוכן ייחודיים לכל עמוד.

### 2. E-E-A-T (מומחיות, ניסיון, סמכות, אמון)
- שורת "נבדק מקצועית ע"י שלומי" + תיבת מחבר + קישור לאודות - `[אוטו]`. *`[פער]` אין תמונת מחבר אמיתית ומספר רשם קבלנים - להוסיף כשיהיו.*
- **ניסיון מהשטח:** מקרה בוחן קצר בגוף ראשון ("קיבלתי טלפון...", "הגענו וגיליתי...") עם מיקום, סיבוך אמיתי ותוצאה, ומספרים שתואמים את טבלת המחירים של העמוד.
- **כתיבה בגובה העיניים:** מומחה שמדבר ללקוח, לא טקסט רובוטי.

### 3. לוקאל (עמודי עיר/אזור)
- אלמנט לוקאלי אמיתי: סוגי בניינים, אקלים (לחות ים, אבק מדברי), שכונות, אתגרים מקומיים - לא שם עיר מוחלף בתבנית.
- מקרה בוחן לוקאלי כשאפשר. בעמודי קבלן עירוני: הביטוי "שיפוצניק ב<עיר>" מופיע 2+ (לא למחוק).

### 4. מבנה וקריאות
- פסקאות 2-4 שורות; בולטים למידע טכני/יתרונות/שלבים.
- היררכיית כותרות: H1 אחד (title) עם הביטוי המרכזי; H2/H3 לוגיים. **לגוון את ניסוח כותרת המחיר** (לא לפתוח כל עמוד ב"כמה עולה").
- מקף קצר רגיל בלבד (-), לעולם לא ארוך/בינוני; **אין אימוג'ים בתוכן**.
- `<PriceTable />` גבוה (אחרי הפתיח או אחרי הסעיף המקומי הראשון); `<Callout type="tip|info|warning">` לטיפים.
- *`[פער]` תמונות: כשיש תמונות אמיתיות מהשטח (לא סטוק) - להטמיע עם Alt מתאים. כרגע עמודי התוכן ללא תמונות פנימיות.*

### 5. קישוריות ומבנה טכני
- **2-4 קישורים פנימיים הקשריים** (anchor text קונטקסטואלי) לעמודים רלוונטיים; קישוריות דו-כיוונית - לקשר גם אל העמוד החדש מעמודים קיימים.
- `parent` להיררכיה; `sidebar` רלוונטי; אין `<` או `{` גולמיים; מחרוזות YAML במרכאות כפולות בלי מרכאות פנימיות (להשתמש ב-״).

### 6. AIO ו-SEO
- **פתיח ישיר (2-3 משפטים)** בראש הגוף: מה השירות + מספר מחיר מטבלת העמוד + למי מתאים + מה העמוד מכסה - כדי שמנועי AI וסניפטים יצטטו.
- **FAQ:** 12+ שאלות בניסוח שגולשים באמת שואלים (חיפוש קולי/AI), תשובות קצרות עם נתונים - `[אוטו]` מרונדר כאקורדיון + FAQPage JSON-LD.
- **מטא:** metaTitle ≤60 תווים עם הביטוי המרכזי פעם אחת + מילת מחיר; description ≤155 תווים עם הביטוי המרכזי פעם אחת + CTA מחיר מסובב ("צפו במחירון" / "ראו מחירים בפנים" / "ראו עלויות" - לא אותו אחד בכל עמוד). ייחודיים לכל עמוד.
- **CTA:** תיבת "מוכנים להתחיל?" עם טלפון/צור-קשר - `[אוטו]` בסוף כל עמוד.
- **עדכניות:** "עודכן לאחרונה" - `[אוטו]`; לרענן `updated` ומחירים לפחות פעם בשנה.
- **Schema:** Service + FAQPage + WebPage(author/reviewedBy) + BreadcrumbList + LocalBusiness גלובלי - `[אוטו]`.
- **מובייל:** פסקאות קצרות ובולטים שנקראים במסך קטן (האתר רספונסיבי).
- **עקביות מחירים:** מחיר של אותו פריט זהה בין עמודים (אב/מחירון/מקור) - אין לסתור.
- **שבירת תבנית:** בלי נוסחת "שלומי ממליץ" חוזרת (קול טיפ ניטרלי), בלי ביטויי מדף חוזרים ("בשורה התחתונה" וכו'), בלי משפטים כפולים בין עמודים.

## פקודות

- `npm run dev` / `npm run build` — הבילד הוא SSG מלא (generateStaticParams)
